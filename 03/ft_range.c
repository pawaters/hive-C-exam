int	ft_abs(int n)
{
	if (n > 0)
		n = n * -1;
	return (n);

int	*ft_range(int start, int end)
{
	char *tab;

	tab = malloc(ft_abs(end - start) + 1);
	if (start > end)
	{
		while (end < start)
			tab[i++] = end++;
			
	}
}	
