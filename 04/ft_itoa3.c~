/*
 * 1) measure len of nb: ft_nblen
 * 2) create function to be able to get the nb to the left: ft_div
 * 3) malloc a str of len=ft_nblen
 * 4) main loop: assign from left to right thanks to div.	
 * CAREFUL: if nbr is max negative, and sign at the front.
 *
 */

#include <stdio.h>
#include <limits.h>

int main(){
	printf("INT_MIN =%d", INT_MIN);
	return (0);
}

