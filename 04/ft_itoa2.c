/*
 * ft_nbr_len: to malloc the str
 * ft_div: to give the 10 multiple to divide the int to give the digit to the left
 *
 * main loop: 
 * div := ft_div;
 * while (nb > 9)
 * str[i] == (nb / div) + '0';
 * div /= 10;
 * i++;  
