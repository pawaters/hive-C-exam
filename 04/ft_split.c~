on top of basics, try using defines instead of ft_strlen
# define WD_NUM 1000
# define WD_LEN 1000

directly malloc for these defines, not exactly for len.

also, divide:
- ft_isspace
- count_words
- malloc_word including len
