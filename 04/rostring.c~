same as split, with different printing:

- count words
- malloc char** tab
- 3 diferent counters: i, j, k.
- print last word first.
- if seg fault, try with define instead or just 100 length and 100 words.
